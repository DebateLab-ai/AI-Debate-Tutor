fastapi==0.104.1
uvicorn[standard]==0.24.0
openai>=1.3.0
pydantic<2
python-dotenv==1.0.0
python-multipart==0.0.6
httpx>=0.24.0
scikit-learn>=1.0.0
supabase>=2.0.0
anthropic>=0.40.0
# PDF report generation for /api/v1/debates/{id}/report.pdf.
# WeasyPrint needs Pango + Cairo at the OS level. macOS: brew install pango.
# Railway/Render: add a nixpacks.toml with libpango-1.0-0, libpangoft2-1.0-0,
# libgdk-pixbuf-2.0-0. If the install fails at boot, app.pdf imports lazily so
# the endpoint returns 503 rather than crashing the whole API.
weasyprint>=60.0,<65.0
jinja2>=3.1.0