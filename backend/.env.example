# OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key_here

# CORS Configuration
# Comma-separated list of allowed origins (e.g., http://localhost:3000,https://your-frontend-url.vercel.app)
# Use * to allow all origins (not recommended for production)
CORS_ORIGINS=http://localhost:3000

# Scoring Model Configuration
# Options: gpt-4o, gpt-4o-mini, gpt-4-turbo
SCORING_MODEL=gpt-4o

# Speech Corpus Directory
# Path to the directory containing debate speech corpus files
SPEECH_CORPUS_DIR=./app/corpus
